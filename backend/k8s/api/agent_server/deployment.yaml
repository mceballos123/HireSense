apiVersion: apps/v1
kind: Deployment
metadata:
  name: agent-server-deployment

spec:
  replicas: 2
  selector:
    matchLabels:
      app: agent-server
  
  template:
    metadata:
      labels:
        app: agent-server
    spec:
      containers:
       - name: agent-server
         image: gcr.io/gen-lang-client-0189309103/agent-server:v1
         ports: 
          - containerPort: 8081
